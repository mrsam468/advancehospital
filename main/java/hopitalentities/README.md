I used the hashset collection to ensure uniqueness in the patient id,
and also I used  map for easy search of patient by passing id and returning patient

I created a generic class that ,add in objects as key and value,also removes object by their key and update an objects and also find object by passing their id 